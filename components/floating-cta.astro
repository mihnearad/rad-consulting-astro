---
// Floating CTA Button that appears on scroll
---

<a id="floating-cta" class="floating-cta" href="/bi-value-calculator" aria-label="Calculate your Business Intelligence ROI">
  <span class="floating-cta-text">Calculate Your ROI</span>
  <svg class="floating-cta-icon" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
    <path d="M9 7h6m0 10v-3m-3 3h.01M9 17h.01M9 14h.01M12 14h.01M15 11h.01M12 11h.01M9 11h.01M7 21h10a2 2 0 002-2V5a2 2 0 00-2-2H7a2 2 0 00-2 2v14a2 2 0 002 2z"/>
  </svg>
</a>

<style>
  .floating-cta {
    position: fixed;
    bottom: 3.5rem;
    right: 2rem;
    background: linear-gradient(135deg, var(--primaryLight) 0%, var(--primary) 100%);
    color: #fff;
    border: none;
    padding: 1rem 1.75rem;
    border-radius: 50px;
    font-size: 1rem;
    font-weight: 700;
    cursor: pointer;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    box-shadow: 0 10px 30px rgba(52, 131, 197, 0.4);
    z-index: 9998;
    opacity: 0;
    visibility: hidden;
    transform: translateY(100px) scale(0.8);
    transition: all 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55);
    touch-action: manipulation;
    text-decoration: none;
  }
  
  .floating-cta.visible {
    opacity: 1;
    visibility: visible;
    transform: translateY(0) scale(1);
  }
  
  .floating-cta:hover {
    transform: translateY(-4px) scale(1.05);
    box-shadow: 0 15px 40px rgba(52, 131, 197, 0.5);
  }
  
  .floating-cta:active {
    transform: translateY(-2px) scale(1.02);
  }
  
  .floating-cta-icon {
    transition: transform 0.3s ease;
  }
  
  .floating-cta:hover .floating-cta-icon {
    transform: translateX(4px);
  }
  
  /* Pulse animation - optimized for mobile performance */
  @keyframes pulse {
    0%, 100% {
      transform: translateY(0) scale(1);
    }
    50% {
      transform: translateY(0) scale(1.05);
    }
  }

  .floating-cta.visible {
    animation: pulse 2s infinite ease-in-out;
  }

  .floating-cta:hover {
    animation: none;
  }
  
  /* Mobile adjustments */
  @media only screen and (max-width: 768px) {
    .floating-cta {
      bottom: 1.5rem;
      right: 1rem;
      padding: 0.875rem 1.5rem;
      font-size: 0.875rem;
    }
    
    .floating-cta-text {
      display: none;
    }
    
    .floating-cta {
      width: 56px;
      height: 56px;
      padding: 0;
      justify-content: center;
      border-radius: 50%;
    }
    
    .floating-cta-icon {
      margin: 0;
    }
  }
  
  /* Hide when modal is open */
  .modal-overlay.active ~ .floating-cta {
    opacity: 0;
    visibility: hidden;
    pointer-events: none;
  }
</style>

<script>
  // Show floating CTA after scrolling down
  const floatingCta = document.getElementById('floating-cta');
  
  function checkScroll() {
    if (!floatingCta) return;
    
    const scrollPosition = window.scrollY;
    const windowHeight = window.innerHeight;
    
    // Show button after scrolling past first viewport
    if (scrollPosition > windowHeight * 0.5) {
      floatingCta.classList.add('visible');
    } else {
      floatingCta.classList.remove('visible');
    }
  }
  
  // Throttle scroll event for performance
  let ticking = false;
  window.addEventListener('scroll', () => {
    if (!ticking) {
      window.requestAnimationFrame(() => {
        checkScroll();
        ticking = false;
      });
      ticking = true;
    }
  });
  
  // Check on load
  checkScroll();
</script>
