---
import { Image } from "astro:assets";
---

<section class="about" id="about">
  <div class="cs-container">
    <div class="about-content">
      <span class="cs-topper">About Us</span>
      <h2 class="cs-title">Small Team, Fast Solutions, Expert Results</h2>
      <p class="cs-text">
        We are dedicated to helping your business succeed. Our team is here to provide you with the best consulting
        services, from IT solutions to data analysis and website design. We
        believe in a collaborative approach to achieve the best results for our
        clients. We operate in Netherlands and Belgium.
      </p>
      <p class="cs-catchphrase">
        We don't bill hours — we engineer outcomes.
      </p>
    </div>
    <div class="about-image">
      <Image
        src="/Mihnea.webp"
        alt="About Us Section"
        width={400}
        height={300}
        class="about-img"
      />
    </div>
  </div>
</section>

<style>
  .about {
    padding: var(--sectionPadding);
    background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
    color: var(--bodyTextColor);
  }

  .about .cs-container {
    width: 100%;
    /* Same max-width as Services section on mobile */
    max-width: 34.375rem;
    margin: auto;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: clamp(3rem, 6vw, 4rem);
  }

  .about-content {
    width: 100%;
    text-align: left;
    order: 1;
  }

  .cs-catchphrase {
    margin-top: 2rem;
    font-size: clamp(1.25rem, 2.5vw, 1.75rem);
    font-weight: 700;
    color: var(--primary);
    font-style: italic;
    line-height: 1.4;
    padding: 1.5rem;
    background: linear-gradient(135deg, rgba(52, 131, 197, 0.08) 0%, rgba(52, 131, 197, 0.05) 100%);
    border-left: 4px solid var(--primary);
    border-radius: 0.5rem;
  }

  .about-image {
    width: 100%;
    display: flex;
    justify-content: center;
    order: 2;
  }

  .about-img {
    width: 100%;
    max-width: 400px;
    height: auto;
    border-radius: 10px;
  }

  /* Tablet - 768px */
  @media only screen and (min-width: 48rem) {
    .about .cs-container {
      max-width: 90rem;
      flex-direction: row;
      align-items: center;
      gap: 20px;
    }

    .about-content {
      flex: 2;
      padding: 20px;
      order: 1;
    }

    .about-image {
      flex: 1;
      padding: 20px;
      order: 2;
    }
  }

  @media (max-width: 768px) {
    .about .cs-container {
      text-align: center;
    }

    .about-content {
      text-align: center;
    }

    .about-content p {
      margin-top: 20px;
      text-align: justify;
    }
  }
</style>
